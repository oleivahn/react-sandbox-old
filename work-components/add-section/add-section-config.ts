const addSectionConfig: IAddSectionConfig = {
  minColumns: 1,
  maxColumns: 30
};

interface IAddSectionConfig {
  minColumns: number;
  maxColumns: number;
}

export default addSectionConfig;
