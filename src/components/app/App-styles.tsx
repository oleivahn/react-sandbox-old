import { createStyles, Theme } from "@material-ui/core/styles";

const styles = (theme: Theme) =>
  createStyles({
    main: {
      margin: 0,
      padding: 0
    },
    rightContainer: {
      marginLeft: "14em"
    }
  });

export default styles;
